@Library('github.com/morgbreth/shared-libraries') _

pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                myPipelineStep()
            }
        }
    }
}
